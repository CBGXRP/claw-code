# Utility Scripts

Cross-platform Node.js utilities for common project tasks.

## Scripts

- `setup.sh` — Initialize project dependencies
- `lint.sh` — Run linter across the codebase
- `test.sh` — Execute full test suite
- `build.sh` — Production build

Add project-specific scripts here. Keep them small, focused, and idempotent.
